import styled from 'styled-components';

export const StatisticsStyle = styled.div`
  .stat-list {
    list-style: none;
    padding: 15px;
  }

  ul {
    list-style: none;
    display: flex;
    padding: 5px;
  }

  li {
  }

  .label {
    display: block;
  }

  .percentage {
    font-size: 20px;
    font-weight: bold;
    display: block;
  }
`;
